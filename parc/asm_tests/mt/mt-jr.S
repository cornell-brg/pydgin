//========================================================================
// mt-jr
//========================================================================

#include "mt-macros.h"

    TEST_MT_BEGIN

    //--------------------------------------------------------------------
    // Basic test
    //--------------------------------------------------------------------

    la $3, 1f
    jr $3
    TEST_CHECK_FAIL
1:

    //--------------------------------------------------------------------
    // Bypassing tests
    //--------------------------------------------------------------------

    TEST_JR_SRC0_BYP( 0, jr )
    TEST_JR_SRC0_BYP( 1, jr )
    TEST_JR_SRC0_BYP( 2, jr )
    TEST_JR_SRC0_BYP( 3, jr )
    TEST_JR_SRC0_BYP( 4, jr )

    TEST_MT_END

